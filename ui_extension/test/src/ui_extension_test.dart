// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';
import 'package:ui_extension/ui_extension.dart';

void main() {
  group('UiExtension', () {
    test('can be instantiated', () {
      expect(UiExtension(), isNotNull);
    });
  });
}
